# Core Java Apps

**Coming up.** Not started yet - this project is scheduled later in the
Jarvis program.

Three Java 8 applications: a Grep app using the Stream API, a JDBC app applying the DAO pattern over PostgreSQL, and a Twitter CRUD app built on the Twitter REST API with OAuth, tested with JUnit and Mockito and packaged with Docker.
